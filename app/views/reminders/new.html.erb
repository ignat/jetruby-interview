<h2>New Reminder</h2>

<%= form_for [@appointment, @reminder] do |f| %>

  <div><label>When (in minutes before appointment) <%= f.text_field :when %></label></div>
  <div><%= f.submit %></div>

<% end %>
